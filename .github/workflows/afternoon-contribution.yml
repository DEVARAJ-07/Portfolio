name: 2. Afternoon Contribution (02:30 PM IST)

on:
  schedule:
    # 02:30 PM IST = 09:00 UTC
    - cron: '0 9 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  afternoon-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main branch
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Configure Git identity
        run: |
          git config --global user.name "DEVARAJ-07"
          git config --global user.email "devakrs07@gmail.com"

      - name: Run Afternoon Contribution Script
        run: |
          COMMIT_MSG=$(node scripts/daily-contribute.js --slot=afternoon)
          echo "Commit message: $COMMIT_MSG"
          git add .github/activity.log
          git commit -m "$COMMIT_MSG"
          git pull --rebase origin main
          git push origin main
